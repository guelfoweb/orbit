```json
{
  "questions": [
    {
      "id": "Q1",
      "question": "What does the module actually do when imported/executed at the top level (module-level side effects)?",
      "why": "The source shows only function definitions with no obvious top-level executable statements, but the exact byte content (1495 bytes) has not been verified by reading the file. Whether importing the module produces any side effect (prints, file writes, network calls) can only be confirmed by reading the actual bytes and/or executing the import in a sandbox."
    },
    {
      "id": "Q2",
      "question": "What is the concrete output of reset_password() for a given email, and what hash function does Python's hash() use in this runtime?",
      "why": "The token is derived from Python's built-in hash(), which is salted per-process for strings (PYTHONHASHSEED) by default. The source alone cannot determine the actual token value or whether it is reproducible across runs; only executing the function in a specific interpreter state can supply the concrete output."
    },
    {
      "id": "Q3",
      "question": "Do the functions reference runtime resources (demo-users.db, uploads/, exports/, reports/, SMTP_PASSWORD env var) that exist or are configured in this environment?",
      "why": "The behavior of find_user, save_upload, export_report, and reset_password depends on external files, directories, and environment variables not present in the source. Whether these exist, their contents, and their effect on execution can only be determined by inspecting the runtime environment."
    }
  ]
}
```